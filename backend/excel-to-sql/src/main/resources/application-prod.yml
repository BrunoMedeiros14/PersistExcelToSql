spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/xlsx_to_sql
    username: root
    password: pass

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
